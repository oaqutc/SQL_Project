# SQL_Project
Yes so this is my project. I assume that you're going to be using a MacBook but it doesn't really matter.

run *pip install flask*

then run *python3 app.py*

then access the page with *http://localhost:5000*
